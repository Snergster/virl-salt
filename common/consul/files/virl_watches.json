
{
  "watches": [
  {
    "type": "service",
    "service": "virl-{{salt['grains.get']('host', 'generic')}}",
    "handler": "/usr/local/bin/hipchat-minor-alarm 'virl status change on {{salt['grains.get']('host', 'generic') }}'"
  }
 ]
}

